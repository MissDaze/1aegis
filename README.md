# Aegis Atlas

Synthetic medication-administration dataset shop. PayPal checkout. Railway Node host.

## Railway

1. New project → this GitHub repo. Leave start command empty.
2. Railway uses the `Dockerfile`. It listens on `$PORT`.
3. GitHub → Settings → Applications → **Railway** → include this repository.

Do not set a start command of `vite` or `npm run dev`.

Shop: sample free · file $29 · commercial $99.
